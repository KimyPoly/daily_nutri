<h1>Groceries List 🧾</h1>
<ul class="ingredients-list" data-controller="ingredient">
<strong><p>All ingredients (<%= @ingredients.count %>): </p></strong>
  <% @ingredients.each do |ingredient| %>
    <li class="grocery-list">
      <label>
        <input type="checkbox" class="custom-checkbox" data-action="click->ingredient#toggleChecked" />
        🔍 <span><%= ingredient %></span>
      </label>
    </li>
  <% end %>
</ul>
<div class="btn-back-and-groceries">
<i class="fa-solid fa-backward"> <%= link_to "Back", program_path%></i>
</div>
