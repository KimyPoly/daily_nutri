<h1>All The Meals</h1>

<%= form_tag(process_meals_program_meals_path, method: :post) do %>
  <% @meals.each do |meal| %>
    <div>
      <%= check_box_tag "selected_meals[]", meal.id %>
      <%= meal.name %>
    </div>
  <% end %>

  <%= submit_tag "Enregistrer mes sélections", onclick: "window.location='#{dashboard_path}'"  %>
<% end %>
